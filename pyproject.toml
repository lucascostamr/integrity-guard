[project]
name = "integrityguard"
version = "0.0.0"
description = ""
authors = [
    {name = "lucascostamr812@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker (>=7.1.0,<8.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
